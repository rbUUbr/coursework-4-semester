<nav class="navbar navbar-findcond navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to 'Lab cloud', root_path, class: 'navbar-brand' %>
    </div>
    <div class="collapse navbar-collapse" id="navbar">
      <%- if user_signed_in? %>
        <%= render 'partials/signed_in_user_uls' %>
      <% else %>
        <%= render 'partials/unsigned_user_uls' %>
      <% end %>
      <%= form_tag root_path, method: 'get', class: 'navbar-form navbar-right search-form' do |f| %>
        <%= text_field_tag :query, params[:query], class: 'form-control', id: 'query' %>
      <% end %>
    </div>
  </div>
</nav>
